<Window x:Class="TreeViewTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeViewTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        --><!-- TreeView --><!--
        <TreeView x:Name="MainTreeView" 
                 ItemsSource="{Binding TreeData}"
                 SelectedItemChanged="TreeView_SelectedItemChanged">

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" Padding="2"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

        --><!-- 状态栏 --><!--
        <StatusBar Grid.Row="1">
            <TextBlock Text="{Binding SelectedNode.Name, StringFormat='选中: {0}'}"/>
        </StatusBar>
    </Grid>-->
    <!--<Window.Resources>
        --><!-- 转换器 --><!--
        <local:TreeNodeTypeToIconConverter x:Key="IconConverter"/>

        --><!-- TreeViewItem样式 --><!--
        <Style x:Key="CustomTreeViewItemStyle" TargetType="{x:Type TreeViewItem}">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,4"/>

            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <TreeView ItemsSource="{Binding TreeData}" 
          ItemContainerStyle="{StaticResource CustomTreeViewItemStyle}">

        <TreeView.ItemTemplate>
            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    --><!-- 图标 --><!--
                    <TextBlock Text="{Binding NodeType, Converter={StaticResource IconConverter}}" 
                          FontFamily="Segoe MDL2 Assets" 
                          FontSize="14" 
                          Margin="0,0,5,0"
                          VerticalAlignment="Center"/>

                    --><!-- 节点名称 --><!--
                    <TextBlock Text="{Binding Name}" 
                          VerticalAlignment="Center"
                          FontSize="12"/>

                    --><!-- 描述信息（可选显示） --><!--
                    <TextBlock Text="{Binding Description}" 
                          Foreground="Gray" 
                          Margin="10,0,0,0"
                          FontSize="10"
                          VerticalAlignment="Center"
                          Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                </StackPanel>
            </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>-->
    
    <Window.DataContext>
        <local:TreeViewModel x:Name="viewmodel"/>
    </Window.DataContext>
    
    <Grid>
        <TreeView ItemsSource="{Binding TreeModel1.Children}">
            
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            
        </TreeView>
    </Grid>
</Window>
