<UserControl x:Class="ProjectCycleManage.View.ProjectStageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProjectCycleManage.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:ProjectStageVM />
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <!-- 样式定义 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8 4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <!-- 主界面 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 标题和项目类型选择 -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="16,16,16,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="项目阶段文档模板管理" 
                              FontSize="20" FontWeight="Bold" 
                              VerticalAlignment="Center" Margin="0,0,16,0"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="项目类型：" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="EquipmentTypeComboBox"
                                 ItemsSource="{Binding EquipmentTypes}"
                                 DisplayMemberPath="EquipmentTypeName"
                                 SelectedItem="{Binding SelectedEquipmentType}"
                                 Style="{StaticResource ComboBoxStyle}"
                                 Width="200"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Column="2" Text="请选择项目类型以开始配置" 
                              Foreground="#666666" FontSize="14"
                              VerticalAlignment="Center"
                              Visibility="{Binding IsEquipmentTypeSelected, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                </Grid>
            </Border>
            
            <!-- 项目阶段卡片区域 -->
            <ScrollViewer Grid.Row="1" Margin="16,8,16,16" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ProjectStages}" 
                             Visibility="{Binding IsEquipmentTypeSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Width="280" Height="180" 
                                   Margin="8" Background="#FAFAFA">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 阶段标题 -->
                                    <TextBlock Grid.Row="0" Text="{Binding ProjectStageName}" 
                                              FontSize="16" FontWeight="Bold" 
                                              HorizontalAlignment="Center" 
                                              Margin="0,0,0,8"/>
                                    
                                    <!-- 阶段描述 -->
                                    <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                              TextWrapping="Wrap" TextAlignment="Center"
                                              Foreground="#666666" FontSize="12"
                                              VerticalAlignment="Center"/>
                                    
                                    <!-- 编辑按钮 -->
                                    <Button Grid.Row="2" Content="编辑文档配置" 
                                           Command="{Binding DataContext.OpenEditModalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.SelectedProjectStage, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="{x:Null}">
                                                        <Setter Property="IsEnabled" Value="True"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        
        <!-- 编辑模态框 -->
        <Border x:Name="EditModal" 
               Background="#80000000" 
               Visibility="{Binding IsEditModalOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
               Panel.ZIndex="1000">
            
            <Border Width="800" Height="600" 
                   Background="White" 
                   CornerRadius="8"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center">
                
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 模态框标题 -->
                    <TextBlock Grid.Row="0" Text="{Binding ModalTitle}" 
                              FontSize="18" FontWeight="Bold" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,16"/>
                    
                    <!-- 内容区域 -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 可用文档类型 -->
                        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="可用文档类型" 
                                          FontSize="14" FontWeight="Bold" 
                                          Margin="0,0,0,8"/>
                                
                                <TextBox Grid.Row="1" Text="{Binding DocumentSearchText}" 
                                        Style="{StaticResource TextBoxStyle}"
                                        Margin="0,0,0,8"/>
                                
                                <ListBox Grid.Row="2" 
                                        ItemsSource="{Binding AvailableDocumentTypes}"
                                        DisplayMemberPath="DocumentTypeName"
                                        ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                                        SelectionMode="Single">
                                    <ListBox.InputBindings>
                                        <MouseBinding MouseAction="LeftDoubleClick" 
                                                     Command="{Binding AddDocumentToStageCommand}"
                                                     CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem}"/>
                                    </ListBox.InputBindings>
                                </ListBox>
                            </Grid>
                        </Border>
                        
                        <!-- 操作按钮 -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Button Content="&gt;&gt;" Style="{StaticResource SecondaryButtonStyle}" 
                                   Command="{Binding AddDocumentToStageCommand}"
                                   CommandParameter="{Binding ElementName=AvailableDocumentsList, Path=SelectedItem}"
                                   Margin="0,4,0,4" Width="40" Height="30"/>
                            
                            <Button Content="&lt;&lt;" Style="{StaticResource SecondaryButtonStyle}" 
                                   Command="{Binding RemoveDocumentFromStageCommand}"
                                   CommandParameter="{Binding ElementName=StageDocumentsList, Path=SelectedItem}"
                                   Margin="0,4,0,4" Width="40" Height="30"/>
                        </StackPanel>
                        
                        <!-- 已选文档类型 -->
                        <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="已选文档类型" 
                                          FontSize="14" FontWeight="Bold" 
                                          Margin="0,0,0,8"/>
                                
                                <ListBox x:Name="StageDocumentsList" Grid.Row="1"
                                        ItemsSource="{Binding StageDocumentTypes}"
                                        DisplayMemberPath="DocumentTypeName"
                                        ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                                        SelectionMode="Single"/>
                            </Grid>
                        </Border>
                    </Grid>
                    
                    <!-- 信息类型配置区域 -->
                    <Grid Grid.Row="1" Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 可用信息类型 -->
                        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="可用信息类型" 
                                          FontSize="14" FontWeight="Bold" 
                                          Margin="0,0,0,8"/>
                                
                                <TextBox Grid.Row="1" Text="{Binding InformationSearchText}" 
                                        Style="{StaticResource TextBoxStyle}"
                                        Margin="0,0,0,8"/>
                                
                                <ListBox Grid.Row="2" 
                                        ItemsSource="{Binding AvailableInformationTypes}"
                                        DisplayMemberPath="Infor"
                                        ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                                        SelectionMode="Single">
                                    <ListBox.InputBindings>
                                        <MouseBinding MouseAction="LeftDoubleClick" 
                                                     Command="{Binding AddInformationToStageCommand}"
                                                     CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=SelectedItem}"/>
                                    </ListBox.InputBindings>
                                </ListBox>
                            </Grid>
                        </Border>
                        
                        <!-- 操作按钮 -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Button Content="&gt;&gt;" Style="{StaticResource SecondaryButtonStyle}" 
                                   Command="{Binding AddInformationToStageCommand}"
                                   CommandParameter="{Binding ElementName=AvailableInformationList, Path=SelectedItem}"
                                   Margin="0,4,0,4" Width="40" Height="30"/>
                            
                            <Button Content="&lt;&lt;" Style="{StaticResource SecondaryButtonStyle}" 
                                   Command="{Binding RemoveInformationFromStageCommand}"
                                   CommandParameter="{Binding ElementName=StageInformationList, Path=SelectedItem}"
                                   Margin="0,4,0,4" Width="40" Height="30"/>
                        </StackPanel>
                        
                        <!-- 已选信息类型 -->
                        <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="已选信息类型" 
                                          FontSize="14" FontWeight="Bold" 
                                          Margin="0,0,0,8"/>
                                
                                <ListBox x:Name="StageInformationList" Grid.Row="1"
                                        ItemsSource="{Binding StageInformationTypes}"
                                        DisplayMemberPath="Infor"
                                        ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                                        SelectionMode="Single"/>
                            </Grid>
                        </Border>
                    </Grid>
                    
                    <!-- 底部按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}" 
                               Command="{Binding CloseEditModalCommand}"
                               Margin="0,0,8,0"/>
                        
                        <Button Content="保存配置" Style="{StaticResource PrimaryButtonStyle}" 
                               Command="{Binding SaveConfigurationCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
