<UserControl x:Class="ProjectCycleManage.View.ProjectStageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProjectCycleManage.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="800" 
             d:DesignWidth="1664"
             Background="#F5F7FA">
    
    <UserControl.DataContext>
        <vm:ProjectStageVM />
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 项目类型选择器样式 (仿年份选择器) -->
        <Style x:Key="EquipmentTypeTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F4FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2D5BD6"/>
                                <Setter Property="Foreground" Value="#2D5BD6"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2D5BD6"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2D5BD6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 卡片样式 -->
        <Style x:Key="StageCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Width" Value="350"/>
            <Setter Property="Height" Value="Auto"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="12" ShadowDepth="4" Direction="270"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="16" ShadowDepth="8" Direction="270"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 主按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D5BD6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A3A8F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 转移按钮样式 -->
        <Style x:Key="TransferButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D5BD6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A3A8F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 项目类型标签样式 -->
        <Style x:Key="TypeTabStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="5" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBox样式 -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <!-- TextBox样式 -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- ListBox项样式 -->
        <Style x:Key="DocumentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="Transparent"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10"
                                Margin="0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Column="0" 
                                        Width="32" Height="32"
                                        Background="#E0E0E0"
                                        CornerRadius="6"
                                        Margin="0,0,10,0">
                                    <TextBlock Text="📄" 
                                              FontSize="16"
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                                </Border>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DocumentTypeName}" 
                                              FontWeight="Medium" 
                                              FontSize="14"/>
                                    <TextBlock Text="{Binding Description}" 
                                              FontSize="12" 
                                              Foreground="#999999"
                                              TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#2D5BD6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 信息项样式 -->
        <Style x:Key="InformationItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="Transparent"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10"
                                Margin="0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Column="0" 
                                        Width="32" Height="32"
                                        Background="#E0E0E0"
                                        CornerRadius="6"
                                        Margin="0,0,10,0">
                                    <TextBlock Text="ℹ️" 
                                              FontSize="16"
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                                </Border>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Infor}" 
                                              FontWeight="Medium" 
                                              FontSize="14"/>
                                    <TextBlock Text="{Binding Description}" 
                                              FontSize="12" 
                                              Foreground="#999999"
                                              TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#2D5BD6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- 主内容区 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 页面标题 -->
            <Border Grid.Row="0" 
                    Padding="20,15"
                    Margin="20,20,20,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" 
                                   Foreground="#2D5BD6"
                                   FontSize="20"
                                   Margin="0,0,8,0"/>
                        <TextBlock Text="项目阶段文档模板管理" 
                                  VerticalAlignment="Center"
                                  FontSize="20" 
                                  FontWeight="SemiBold" 
                                  Foreground="#2D5BD6"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 项目类型选择器 (仿年份选择器样式) -->
            <Border Grid.Row="1" Margin="20,10,20,0">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding EquipmentTypes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton Content="{Binding EquipmentTypeName}" 
                                           Style="{StaticResource EquipmentTypeTabStyle}"
                                           GroupName="EquipmentTypeTabs"
                                           IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                           Command="{Binding DataContext.SelectEquipmentTypeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
            
            <!-- 阶段文档卡片区域 -->
            <ScrollViewer Grid.Row="2" 
                         Margin="10,10,10,20" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding ProjectStages}"
                             Visibility="{Binding IsEquipmentTypeSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StageCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 卡片头部 -->
                                    <Grid Grid.Row="0" Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" 
                                                  Text="{Binding ProjectStageName}" 
                                                  FontSize="18" 
                                                  FontWeight="SemiBold" 
                                                  Foreground="#2D5BD6"/>
                                        
                                        <Border Grid.Column="1" 
                                               Background="#E3F2FD"
                                               CornerRadius="20"
                                               Padding="12,4">
                                            <TextBlock Text="{Binding DocumentCount, StringFormat='{}文档数: {0}'}" 
                                                      FontSize="12" 
                                                      FontWeight="Medium"
                                                      Foreground="#2196F3"/>
                                        </Border>
                                    </Grid>
                                    
                                    <!-- 分隔线 -->
                                    <Border Grid.Row="1" 
                                           Height="1" 
                                           Background="#E0E0E0" 
                                           Margin="0,0,0,15"/>
                                    
                                    <!-- 文档列表 -->
                                    <Grid Grid.Row="2" Margin="0,0,0,15">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                     MaxHeight="150">
                                            <ItemsControl ItemsSource="{Binding Documents}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,10">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            
                                                            <Border Grid.Column="0" 
                                                                   Width="36" Height="36"
                                                                   Background="#E0E0E0"
                                                                   CornerRadius="6"
                                                                   Margin="0,0,10,0">
                                                                <TextBlock Text="{Binding Icon}" 
                                                                          FontSize="18"
                                                                          HorizontalAlignment="Center" 
                                                                          VerticalAlignment="Center"/>
                                                            </Border>
                                                            
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding Name}" 
                                                                          FontWeight="Medium" 
                                                                          FontSize="14"
                                                                          TextTrimming="CharacterEllipsis"/>
                                                                <TextBlock Text="{Binding Description}" 
                                                                          FontSize="12" 
                                                                          Foreground="#999999"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                        
                                        <!-- 空状态提示 -->
                                        <TextBlock Text="请点击编辑按钮配置文档"
                                                  FontSize="12"
                                                  Foreground="#999999"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Documents.Count}" Value="0">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                    
                                    <!-- 编辑按钮 -->
                                    <Button Grid.Row="3" 
                                           Content="✏️ 编辑文档" 
                                           Style="{StaticResource PrimaryButtonStyle}"
                                           HorizontalAlignment="Right"
                                           Command="{Binding DataContext.OpenEditModalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding Stage}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        
        <!-- 编辑模态框 -->
        <Border x:Name="EditModal" 
               Background="#80000000" 
               Visibility="{Binding IsEditModalOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
               Panel.ZIndex="1000">
            
            <Border Width="1400" 
                   MaxHeight="900" 
                   Background="White" 
                   CornerRadius="12"
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="30" ShadowDepth="10" Direction="270"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 模态框头部 -->
                    <Border Grid.Row="0" 
                           Background="#F9F9F9" 
                           BorderBrush="#E0E0E0" 
                           BorderThickness="0,0,0,1"
                           Padding="20">
                        <Grid>
                            <TextBlock Text="{Binding ModalTitle}" 
                                      FontSize="20" 
                                      FontWeight="SemiBold" 
                                      Foreground="#2D5BD6"
                                      VerticalAlignment="Center"/>
                            
                            <Button Content="✕" 
                                   FontSize="20"
                                   Foreground="#666666"
                                   Background="Transparent"
                                   BorderThickness="0"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Cursor="Hand"
                                   Command="{Binding CloseEditModalCommand}"/>
                        </Grid>
                    </Border>
                    
                    <!-- 模态框内容区 - 三栏布局 -->
                    <Grid Grid.Row="1" Margin="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- 左侧面板 - 可用文档与信息 -->
                        <Border Grid.Column="0" 
                               BorderBrush="#E0E0E0" 
                               BorderThickness="0,0,1,0"
                               Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 标题 -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="🗄️" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="可用文档与信息" 
                                              FontSize="16" 
                                              FontWeight="SemiBold" 
                                              Foreground="#2D5BD6"/>
                                </StackPanel>
                                
                                <!-- 搜索框 -->
                                <TextBox Grid.Row="1" 
                                        Text="{Binding DocumentSearchText, UpdateSourceTrigger=PropertyChanged}" 
                                        Style="{StaticResource SearchBoxStyle}"
                                        Margin="0,0,0,15">
                                    <TextBox.Resources>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource SearchBoxStyle}">
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                                <VisualBrush.Visual>
                                                                    <TextBlock Text="🔍 搜索文档或信息..." 
                                                                              Foreground="#999999" 
                                                                              Margin="35,0,0,0"/>
                                                                </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Resources>
                                </TextBox>
                                
                                <!-- 列表容器 -->
                                <Grid Grid.Row="2">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="15"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 文档模板列表 -->
                                    <Border Grid.Row="0" 
                                           BorderBrush="#E0E0E0" 
                                           BorderThickness="1" 
                                           CornerRadius="8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            
                                            <Border Grid.Row="0" 
                                                   Background="#F5F7FA" 
                                                   BorderBrush="#E0E0E0" 
                                                   BorderThickness="0,0,0,1"
                                                   Padding="15,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📄" FontSize="14" Margin="0,0,8,0"/>
                                                    <TextBlock Text="文档模板" 
                                                              FontWeight="SemiBold" 
                                                              Foreground="#2D5BD6"/>
                                                </StackPanel>
                                            </Border>
                                            
                                            <ListBox x:Name="AvailableDocumentsList"
                                                    Grid.Row="1" 
                                                    ItemsSource="{Binding AvailableDocumentTypes}"
                                                    ItemContainerStyle="{StaticResource DocumentItemStyle}"
                                                    SelectionMode="Single"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="10"
                                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                                <ListBox.InputBindings>
                                                    <MouseBinding MouseAction="LeftDoubleClick" 
                                                                 Command="{Binding AddDocumentToStageCommand}"
                                                                 CommandParameter="{Binding ElementName=AvailableDocumentsList, Path=SelectedItem}"/>
                                                </ListBox.InputBindings>
                                            </ListBox>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- 信息模板列表 -->
                                    <Border Grid.Row="2" 
                                           BorderBrush="#E0E0E0" 
                                           BorderThickness="1" 
                                           CornerRadius="8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            
                                            <Border Grid.Row="0" 
                                                   Background="#F5F7FA" 
                                                   BorderBrush="#E0E0E0" 
                                                   BorderThickness="0,0,0,1"
                                                   Padding="15,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,8,0"/>
                                                    <TextBlock Text="信息模板" 
                                                              FontWeight="SemiBold" 
                                                              Foreground="#2D5BD6"/>
                                                </StackPanel>
                                            </Border>
                                            
                                            <ListBox x:Name="AvailableInformationList"
                                                    Grid.Row="1" 
                                                    ItemsSource="{Binding AvailableInformationTypes}"
                                                    ItemContainerStyle="{StaticResource InformationItemStyle}"
                                                    SelectionMode="Single"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="10"
                                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                                <ListBox.InputBindings>
                                                    <MouseBinding MouseAction="LeftDoubleClick" 
                                                                 Command="{Binding AddInformationToStageCommand}"
                                                                 CommandParameter="{Binding ElementName=AvailableInformationList, Path=SelectedItem}"/>
                                                </ListBox.InputBindings>
                                            </ListBox>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                        
                        <!-- 中间面板 - 转移按钮 -->
                        <Border Grid.Column="1" Background="#F9F9F9" Padding="20">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Button Content="➡️" 
                                       Style="{StaticResource TransferButtonStyle}"
                                       ToolTip="添加到阶段"
                                       Command="{Binding AddDocumentToStageCommand}"
                                       CommandParameter="{Binding ElementName=AvailableDocumentsList, Path=SelectedItem}"/>
                                
                                <Button Content="⬅️" 
                                       Style="{StaticResource TransferButtonStyle}"
                                       ToolTip="从阶段移除"
                                       Command="{Binding RemoveDocumentFromStageCommand}"
                                       CommandParameter="{Binding ElementName=StageDocumentsList, Path=SelectedItem}"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- 右侧面板 - 当前阶段配置 -->
                        <Border Grid.Column="2" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 标题 -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="当前阶段配置" 
                                              FontSize="16" 
                                              FontWeight="SemiBold" 
                                              Foreground="#2D5BD6"/>
                                </StackPanel>
                                
                                <!-- 列表容器 -->
                                <Grid Grid.Row="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="15"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 阶段文档列表 -->
                                    <Border Grid.Row="0" 
                                           BorderBrush="#E0E0E0" 
                                           BorderThickness="1" 
                                           CornerRadius="8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            
                                            <Border Grid.Row="0" 
                                                   Background="#F5F7FA" 
                                                   BorderBrush="#E0E0E0" 
                                                   BorderThickness="0,0,0,1"
                                                   Padding="15,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="📄" FontSize="14" Margin="0,0,8,0"/>
                                                    <TextBlock Text="阶段文档" 
                                                              FontWeight="SemiBold" 
                                                              Foreground="#2D5BD6"/>
                                                </StackPanel>
                                            </Border>
                                            
                                            <ListBox x:Name="StageDocumentsList"
                                                    Grid.Row="1" 
                                                    ItemsSource="{Binding StageDocumentTypes}"
                                                    ItemContainerStyle="{StaticResource DocumentItemStyle}"
                                                    SelectionMode="Single"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="10"
                                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- 阶段信息列表 -->
                                    <Border Grid.Row="2" 
                                           BorderBrush="#E0E0E0" 
                                           BorderThickness="1" 
                                           CornerRadius="8">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            
                                            <Border Grid.Row="0" 
                                                   Background="#F5F7FA" 
                                                   BorderBrush="#E0E0E0" 
                                                   BorderThickness="0,0,0,1"
                                                   Padding="15,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,8,0"/>
                                                    <TextBlock Text="阶段信息" 
                                                              FontWeight="SemiBold" 
                                                              Foreground="#2D5BD6"/>
                                                </StackPanel>
                                            </Border>
                                            
                                            <ListBox x:Name="StageInformationList"
                                                    Grid.Row="1" 
                                                    ItemsSource="{Binding StageInformationTypes}"
                                                    ItemContainerStyle="{StaticResource InformationItemStyle}"
                                                    SelectionMode="Single"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="10"
                                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                    
                    <!-- 模态框底部 -->
                    <Border Grid.Row="2" 
                           Background="#F9F9F9" 
                           BorderBrush="#E0E0E0" 
                           BorderThickness="0,1,0,0"
                           Padding="20">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="取消" 
                                   Style="{StaticResource SecondaryButtonStyle}" 
                                   Command="{Binding CloseEditModalCommand}"
                                   Margin="0,0,10,0"/>
                            
                            <Button Content="保存配置" 
                                   Style="{StaticResource PrimaryButtonStyle}" 
                                   Command="{Binding SaveConfigurationCommand}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
