<UserControl x:Class="ScrollControlProject.SeamlessLoopingScroll"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScrollControlProject"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <!-- 数据项样式 -->
        <DataTemplate x:Key="ProjectItemTemplate">
            <Grid Margin="10,5" Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <!-- 状态圆环 -->
                <Ellipse Grid.Column="0" Width="16" Height="16" Stroke="{Binding StatusColor}" StrokeThickness="2"/>
                
                <!-- 项目名称 -->
                <TextBlock Grid.Column="1" Text="{Binding ProjectName}" VerticalAlignment="Center" FontSize="14"/>
                
                <!-- 进度与资料 -->
                <StackPanel Grid.Column="2" Orientation="Vertical">
                    <ProgressBar Value="{Binding Progress}" Height="8" Margin="5,0"/>
                    <TextBlock Text="{Binding Materials}" FontSize="12" Foreground="Gray"/>
                </StackPanel>
                
                <!-- 负责人 -->
                <TextBlock Grid.Column="3" Text="{Binding Owner}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <!-- 滚动容器 -->
        <Border BorderBrush="#DDDDDD" BorderThickness="1" Margin="10">
            <Grid>
                <!-- 遮罩层，用于创建渐隐效果 -->
                <Rectangle VerticalAlignment="Top" Height="20">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#FFFFFFFF" Offset="0"/>
                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <Rectangle VerticalAlignment="Bottom" Height="20">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#00FFFFFF" Offset="0"/>
                            <GradientStop Color="#FFFFFFFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- 数据展示区域 -->
                <ScrollViewer x:Name="scroller" VerticalScrollBarVisibility="Hidden" 
                              HorizontalScrollBarVisibility="Disabled" 
                              IsHitTestVisible="False">
                    <ItemsControl x:Name="itemsControl" ItemTemplate="{StaticResource ProjectItemTemplate}">
                        <!-- 应用数据模板 -->
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
