<Window x:Class="ProjectManagementSoftwareFrame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectManagementSoftwareFrame"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Margin="30 10 30 10"
                        Content="连接数据库"/>

                <Button Grid.Column="0"
                        Grid.Row="1"
                        Margin="30 10 30 10"
                        Content="查询所有"/>

                <Button Grid.Column="0"
                        Margin="30 10 30 10"
                        Grid.Row="2"
                        Content="查询2022"/>

                <Button Grid.Column="1"
                        Grid.Row="1"
                        Margin="30 10 30 10"
                        Content="导入数据"/>

                <TextBlock Grid.Row="2"
                           Margin="30 10 30 10"
                           Grid.Column="0"
                           Grid.ColumnSpan="5"/>
            </Grid>



            <Grid Grid.Row="1">
                <DataGrid x:Name="dataGrid" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="10">
                    <DataGrid.Columns>
                        <!-- 文本列 -->
                        <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="*"/>

                        <!-- 数字列 -->
                        <DataGridTextColumn Header="年龄" Binding="{Binding Age}" Width="80"/>

                        <!-- 复选框列 -->
                        <DataGridCheckBoxColumn Header="已婚" Binding="{Binding IsMarried}" Width="80"/>

                        <!-- 组合框列 -->
                        <DataGridComboBoxColumn Header="部门" 
                                      SelectedItemBinding="{Binding Department}"
                                      Width="120"/>

                        <!-- 模板列 -->
                        <DataGridTemplateColumn Header="入职日期" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding HireDate, StringFormat=yyyy-MM-dd}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding HireDate}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>
