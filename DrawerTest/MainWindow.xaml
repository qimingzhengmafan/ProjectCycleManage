<Window x:Class="DrawerTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:DrawerTest"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:DrawerUIVM/>
    </Window.DataContext>
    <ScrollViewer>
        <local:DrawerUI/>
    </ScrollViewer>
    
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        --><!--<TextBox Height="50"
                 x:Name="tt"
                 Text="0"/>-->

        <!--<local:ProjectProgressInfo />-->


        <!--<ScrollViewer>
            <StackPanel Margin="20" VerticalAlignment="Top">

                --><!-- 短文本 --><!--
                <local:ProjectProgressInfo 
                        ContentText="短文本"
                        DisplayTime="{x:Static system:DateTime.Now}"
                        BeltColor="LightBlue"
                        Margin="5"/>

                --><!-- 中等长度文本 --><!--
                <local:ProjectProgressInfo 
                ContentText="中等长度文本"
                DisplayTime="{x:Static system:DateTime.Now}"
                BeltColor="LightGreen"
                Margin="5"/>

                --><!-- 长文本 --><!--
                <local:ProjectProgressInfo 
                ContentText="这是一个非常长的文本内容，用于测试同步带控件的自适应宽度功能"
                DisplayTime="{x:Static system:DateTime.Now}"
                BeltColor="LightCoral"
                Margin="5"/>

                --><!-- 多行文本 --><!--
                <local:ProjectProgressInfo 
                ContentText="第一行文本&#x0a;第二行文本"
                DisplayTime="{x:Static system:DateTime.Now}"
                BeltColor="LightGoldenrodYellow"
                Margin="5"/>

                --><!-- 固定宽度 --><!--
                <local:ProjectProgressInfo 
                ContentText="固定宽度文本"
                DisplayTime="{x:Static system:DateTime.Now}"
                BeltColor="LightPink"
                FixedWidth="200"
                Margin="5"/>

                --><!-- 动态更新示例 --><!--
                <local:ProjectProgressInfo x:Name="DynamicControl"
                ContentText="初始文本"
                BeltColor="LightGray"
                Margin="5"/>

                <StackPanel Orientation="Horizontal" Margin="10">
                    <Button Content="短文本" Click="UpdateShortText_Click" Margin="5"/>
                    <Button Content="长文本" Click="UpdateLongText_Click" Margin="5"/>
                    <Button Content="切换颜色" Click="ChangeColor_Click" Margin="5"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>--><!--

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Border Background="#f9fafb"
                        VerticalAlignment="Top"
                        Height="92"
                        CornerRadius="0 0 30 30">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        


                    </Grid>
                </Border>
                <Grid Height="{Binding Text,ElementName=tt}">
                    <StackPanel>
                        <Grid Background="#F2F2F2"
                              Height="60">

                        </Grid>
                        <Grid Background="White"
                              Height="60">

                        </Grid>
                        <Grid Background="#F2F2F2"
                              Height="60">

                        </Grid>
                        <Grid Background="White"
                              Height="60">

                        </Grid>
                        <Grid Background="#F2F2F2"
                              Height="60">

                        </Grid>
                        <Grid Background="White"
                              Height="60">

                        </Grid>
                    </StackPanel>
                </Grid>
                <Border Background="#f9fafb"
        VerticalAlignment="Top"
        Height="92"
        CornerRadius="0 0 30 30">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="2024"
                       FontSize="20"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="70 0 0 0"/>
                            <Label Content="年"
                   FontSize="20"
                   FontWeight="Bold"
                   VerticalContentAlignment="Center"
                   Margin="10 0 0 0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">
                            <Label Content="总项目数 : "
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="66 0 0 0"
                   VerticalContentAlignment="Center"/>
                            <TextBlock Text="18"
                       FontSize="15"
                       FontWeight="Bold"
                       Margin="30 0 0 0"
                       VerticalAlignment="Center"/>
                            <Label Content="个"
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="20 0 0 0"
                   VerticalContentAlignment="Center"/>

                            <Label Content="|"
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="50 0 0 0"
                   VerticalContentAlignment="Center"/>

                            <Label Content="已完成 : "
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="50 0 0 0"
                   VerticalContentAlignment="Center"/>
                            <TextBlock Text="12"
                       FontSize="15"
                       FontWeight="Bold"
                       Foreground="#16A34A"
                       Margin="30 0 0 0"
                       VerticalAlignment="Center"/>
                            <Label Content="个"
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="20 0 0 0"
                   VerticalContentAlignment="Center"/>
                        </StackPanel>


                    </Grid>
                </Border>
                <Grid Height="{Binding Text,ElementName=tt}">
                    <StackPanel>
                        <Grid Background="#F2F2F2"
              Height="60">

                        </Grid>
                        <Grid Background="White"
              Height="60">

                        </Grid>
                        <Grid Background="#F2F2F2"
              Height="60">

                        </Grid>
                        <Grid Background="White"
              Height="60">

                        </Grid>
                        <Grid Background="#F2F2F2"
              Height="60">

                        </Grid>
                        <Grid Background="White"
              Height="60">

                        </Grid>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>

    </Grid>-->

</Window>
